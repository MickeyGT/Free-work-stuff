#include<fstream>
#include<string.h>
#include<ctype.h>
#include<iostream>
#include<algorithm>
#include<map>
#include<unordered_map>
#include<array>
#include<deque>
#include<queue>
#include<math.h>
#include<functional>
#include<unordered_set>
#include<set>
#include<iomanip>
#include<bitset>
using namespace std;
int n, t,k,i,m,x,y;
vector<int>v[50500];
int main()
{
	ifstream f("file.in");
	ofstream g("file.out");
	//ifstream f("progr.in");
	//ofstream g("progr.out");
	f >>n>>m;
	for (i = 1; i <= m; i++)
	{
		f >> x >> y;
		v[x].push_back(y);
	}
	return 0;
}
