#include<stdlib.h>
#include<stdio.h>
#include<fstream>
#include<string.h>
#include<deque>
#include<algorithm>
deque <long, int> D;
using namespace std;
long n, k,i,el;

int main()
{
	ifstream f("deque.in");
	ofstream g("deque.out");
	f >> n >> k;
	f >> el;
	D.push_front(el);
	for (i = 1;i <= n-1;i++)
	{
		f >> el;
	}

	return 0;
}
